# 语译 · 部署手册

本文档覆盖本地、局域网和公网三种部署方式，以及环境变量、方言识别、HTTPS、安全配置和常见问题。

## 1. 项目结构

```text
hackson/
├── frontend/             # 前端 React + Vite
├── backend/              # 后端 Node.js + Express
├── deploy/
│   ├── funasr/           # 方言语音识别服务
│   ├── upload.sh         # 轻量上传脚本
│   └── DEPLOY.md         # 简要部署说明
├── demo/                 # 自动演示脚本
├── start-production.sh   # 生产启动脚本
├── yuyi-deploy-package.tar.gz  # 已生成的部署包
├── PROJECT.md
└── README.md
```

## 2. 本地部署

### 2.1 安装依赖

后端：

```bash
cd /Users/a1/Documents/ChatGPT/hackson/backend
npm install
```

前端：

```bash
cd /Users/a1/Documents/ChatGPT/hackson/frontend
npm install
```

方言识别服务：

```bash
cd /Users/a1/Documents/ChatGPT/hackson/deploy/funasr
bash run.sh
```

首次运行会自动下载 FunASR 模型，耗时较长。

### 2.2 配置后端密钥

```bash
cd /Users/a1/Documents/ChatGPT/hackson/backend
cp .env.example .env
nano .env
```

至少填写：

```bash
MODEL_API_KEY=你的DeepSeek密钥
MODEL_BASE_URL=https://api.deepseek.com
MODEL_NAME=deepseek-chat
MODEL_TIMEOUT_MS=30000
PORT=3001
ASR_API_URL=http://127.0.0.1:10095
ASR_API_KEY=
ASR_MODEL=sensevoice
```

### 2.3 启动

启动顺序：

1. 方言识别服务
2. 后端
3. 前端

```bash
# 终端 1
cd /Users/a1/Documents/ChatGPT/hackson/deploy/funasr
bash run.sh

# 终端 2
cd /Users/a1/Documents/ChatGPT/hackson/backend
npm run dev

# 终端 3
cd /Users/a1/Documents/ChatGPT/hackson/frontend
npm run dev
```

访问：

- 前端：`https://localhost:5173`
- 后端：`http://127.0.0.1:3001`
- 方言识别：`http://127.0.0.1:10095`

### 2.4 本地生产模式

```bash
cd /Users/a1/Documents/ChatGPT/hackson
./start-production.sh
```

访问：

```text
http://localhost:3001
```

## 3. 局域网部署

### 3.1 配置

确保以下服务监听 `0.0.0.0`：

- `frontend/vite.config.js`：`host: "0.0.0.0"`
- `backend/server.js`：`app.listen(PORT, "0.0.0.0")`
- `deploy/funasr/run.sh`：`--host 0.0.0.0`

### 3.2 查看电脑局域网 IP

```bash
ifconfig | grep 'inet ' | grep -v 127.0.0.1
```

例如：

```text
10.71.125.175
```

### 3.3 访问地址

```text
前端：https://10.71.125.175:5173
后端：http://10.71.125.175:3001
方言识别：http://10.71.125.175:10095
```

### 3.4 注意

- IP 变化后需要更换访问地址。
- 手机通过局域网 HTTP 访问时，麦克风可能不可用；建议使用 HTTPS。
- HTTPS 依赖 `frontend/certs/` 下的本地自签名证书。

## 4. 公网部署

### 4.1 方案 A：只上传前端，Mac 做后端

适合演示和临时测试。

#### 4.1.1 给 Mac 后端开隧道

```bash
cloudflared tunnel --url http://127.0.0.1:3001
```

会得到一个后端公网地址：

```text
https://xxxx.trycloudflare.com
```

#### 4.1.2 构建前端

```bash
cd /Users/a1/Documents/ChatGPT/hackson/frontend
VITE_API_BASE=https://xxxx.trycloudflare.com npm run build
```

#### 4.1.3 上传前端 dist

把 `frontend/dist` 上传到任意静态服务器，例如 Vercel、Netlify、Nginx、OSS。

#### 4.1.4 配置 CORS

在 Mac 后端启动时允许前端域名：

```bash
CORS_ORIGINS=https://你的前端域名 npm start
```

#### 4.1.5 注意

- Mac 必须保持开机。
- 后端和 FunASR 必须保持运行。
- 临时隧道不稳定，正式演示建议使用命名隧道或 frp。

### 4.2 方案 B：完整部署到服务器

#### 4.2.1 上传

方式一：上传完整包

```bash
scp yuyi-deploy-package.tar.gz user@服务器IP:/opt/
```

方式二：轻量 rsync 上传

```bash
export DEPLOY_USER=root
export DEPLOY_HOST=服务器IP
export DEPLOY_PATH=/opt/yuyi

bash deploy/upload.sh
```

#### 4.2.2 服务器安装

```bash
cd /opt/yuyi
cd backend
npm install

cd ../frontend
npm install
npm run build

cd ..
./start-production.sh
```

#### 4.2.3 配置环境变量

```bash
cd /opt/yuyi/backend
cp .env.example .env
nano .env
```

#### 4.2.4 配置方言识别

如果需要语音、音频、视频和方言识别：

```bash
cd /opt/yuyi/deploy/funasr
bash run.sh
```

然后确保后端 `.env`：

```bash
ASR_API_URL=http://127.0.0.1:10095
```

如果不想在服务器运行 3GB 模型，可接入云端 ASR。

### 4.3 方案 C：平台托管

适合 Vercel、Netlify、Railway、Render 等平台。

前端使用 `frontend/dist`，后端使用 Node.js 服务。

需要在平台配置环境变量：

```bash
MODEL_API_KEY
MODEL_BASE_URL
MODEL_NAME
PORT
ASR_API_URL
ASR_API_KEY
ASR_MODEL
CORS_ORIGINS
```

## 5. 环境变量说明

| 变量 | 说明 | 默认值 |
| --- | --- | --- |
| `MODEL_API_KEY` | 大模型 API Key | 空 |
| `MODEL_BASE_URL` | 大模型接口地址 | `https://api.deepseek.com` |
| `MODEL_NAME` | 模型名 | `deepseek-chat` |
| `MODEL_TIMEOUT_MS` | 请求超时 | `30000` |
| `PORT` | 后端端口 | `3001` |
| `ASR_API_URL` | 方言语音识别地址 | 空 |
| `ASR_API_KEY` | 语音识别 Key | 空 |
| `ASR_MODEL` | 语音识别模型 | `whisper-1` |
| `CORS_ORIGINS` | CORS 白名单 | 空 |
| `RATE_LIMIT` | 每分钟每 IP 请求上限 | `120` |

## 6. 测试

后端：

```bash
cd /Users/a1/Documents/ChatGPT/hackson/backend
npm test
```

前端：

```bash
cd /Users/a1/Documents/ChatGPT/hackson/frontend
npm test
npm run build
```

自动演示：

```bash
cd /Users/a1/Documents/ChatGPT/hackson
./demo/demo.sh
```

## 7. 常见问题

### 7.1 前端打不开

- 确认是否使用 HTTPS。
- 检查 Vite 是否运行。
- 检查证书是否存在：`frontend/certs/`。

### 7.2 翻译接口失败

- 检查 `backend/.env` 中 `MODEL_API_KEY`。
- 检查后端是否运行在 3001。
- 检查 CORS 是否允许前端域名。

### 7.3 语音/视频/音频不可用

- 检查 FunASR 是否运行。
- 检查 `ASR_API_URL` 是否正确。
- 检查服务器是否已下载 FunASR 模型。

### 7.4 图片识别失败

- 检查 `frontend/public/tesseract` 是否存在。
- 检查浏览器是否能访问本地 worker/core。

### 7.5 局域网 IP 变了

重新执行：

```bash
ifconfig | grep 'inet ' | grep -v 127.0.0.1
```

并用新 IP 访问。

### 7.6 公网临时隧道不稳定

建议改用：

- Cloudflare 命名隧道
- frp
- ngrok
- 有公网 IP 的服务器

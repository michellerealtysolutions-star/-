# 语译 · 代际翻译器项目文档

## 1. 项目定位

语译是一款面向代际沟通的网页工具。它不是传统中英互译，而是把年轻人与长辈之间的表达方式互相转译，帮助双方听懂彼此真正想说的话。

核心场景：

- 年轻人说网络梗、缩写、情绪化表达，长辈听不懂。
- 长辈用老式说法、方言或催促语气，年轻人觉得被冒犯或唠叨。
- 同一个输入框支持中英文，也支持文字、语音、图片、视频多种输入。

## 2. 核心功能

### 2.1 代际翻译

- 中文：年轻话 ↔ 长辈话
- 英文：年轻英语 ↔ 长辈英语
- 自动判断语言
- 自动判断年轻表达、长辈表达或无需翻译
- 输出成品译文和沟通说明

### 2.2 梗识别

- 独立识别网络梗、缩写、谐音梗、圈层黑话
- 返回梗类别和具体含义
- 单独出现的梗不强行翻译，只解释
- 句内多个梗会合并为独立 `meme` 字段

### 2.3 方言与老式表达

- 支持粤语、四川话、东北话、河南话、上海话等方言识别
- 演示素材包含年轻粤语和长辈式粤语
- 老式表达会被识别为 `elder_to_young`，并转成更自然的年轻说法

### 2.4 多模态输入

- 文字输入
- 麦克风录音
- 音频文件上传
- 图片 OCR
- 视频上传并提取音频后转写

### 2.5 安全能力

- 色情、暴力、政治敏感内容拦截
- 辱骂、人身攻击内容拦截
- 支持 `傻。逼`、`傻 逼`、`f u c k` 等符号绕过写法
- 可选大模型语义安全第二层，默认关闭
- XSS、SQL 注入、乱码、非中英文输入拦截

### 2.6 体验功能

- 译文朗读
- 历史记录与收藏
- 大字号模式
- 自动演示
- 一键演示脚本
- 反馈收集

## 3. 技术架构

### 3.1 前端

- React 18
- Vite
- Tesseract.js 本地 OCR
- 浏览器 MediaRecorder
- 浏览器 SpeechSynthesis

### 3.2 后端

- Node.js
- Express
- Multer 视频上传
- DeepSeek 兼容大模型接口
- FunASR 本地方言语音识别
- ffmpeg 视频音频提取

### 3.3 目录结构

```text
hackson/
├── frontend/                     # React + Vite 前端
│   ├── public/
│   │   ├── demo/                 # 演示音频、梗图、视频
│   │   └── tesseract/            # OCR worker、core、本地语言包
│   └── src/
├── backend/                      # Node.js + Express 后端
│   ├── routes/
│   ├── services/
│   ├── prompts/
│   ├── config/
│   ├── scripts/                  # 反馈分析工具
│   └── data/                     # 反馈数据与生成报告
├── deploy/funasr/                # 方言语音识别服务
├── demo/demo.sh                  # 一键演示脚本
├── smoke.sh                      # 一键工程冒烟测试
├── start-production.sh           # 生产启动脚本
├── yuyi-deploy-package.tar.gz    # 部署包
├── 项目文档.md                    # 本文档
└── 部署手册.md
```

## 4. 接口设计

### `POST /api/translate`

代际翻译接口。

请求示例：

```json
{
  "text": "我 emo 了，想摆烂",
  "direction": "auto",
  "dialect": null,
  "tone": "gentle"
}
```

响应示例：

```json
{
  "translation": "我心情不太好，想休息一下，什么都不想干。",
  "subtext": "“emo”指情绪低落，“摆烂”指放弃努力。",
  "meme": {
    "category": "情绪/缩写梗 / 网络流行语",
    "meaning": "emo：情绪低落；摆烂：不再努力，先歇一歇。"
  },
  "tone": "gentle",
  "dialect_used": null,
  "detected_direction": "young_to_elder",
  "needs_translation": true
}
```

### `POST /api/transcribe`

音频转写接口，接收 `audio_base64` 和 `mime`。

### `POST /api/transcribe/video`

视频转写接口，使用 `multipart/form-data` 上传 `file` 字段。后端用 ffmpeg 提取音频后交给 FunASR。

### `POST /api/moderate`

图片 OCR 文本安全预检。

### `POST /api/report`

提交错误反馈。

### `GET /api/reports/stats`

反馈数量统计。

### `GET /health`

健康检查。

## 5. 环境变量

后端 `.env` 主要配置：

| 变量 | 说明 | 默认值 |
| --- | --- | --- |
| `MODEL_API_KEY` | 大模型 API Key | 空 |
| `MODEL_BASE_URL` | OpenAI 兼容接口地址 | `https://api.deepseek.com` |
| `MODEL_NAME` | 模型名 | `deepseek-chat` |
| `MODEL_TIMEOUT_MS` | 模型请求超时 | `30000` |
| `PORT` | 后端端口 | `3001` |
| `ASR_API_URL` | 方言语音识别接口地址 | 空 |
| `ASR_API_KEY` | 语音识别 Key | 空 |
| `ASR_MODEL` | 语音识别模型名 | `whisper-1` |
| `SAFETY_MODEL_ENABLED` | 是否启用大模型语义安全第二层 | `false` |
| `SAFETY_MODEL_NAME` | 语义安全模型名 | 空 |
| `SAFETY_MODEL_TIMEOUT_MS` | 语义安全超时 | `8000` |
| `CORS_ORIGINS` | 可选 CORS 白名单 | 空 |
| `RATE_LIMIT` | 每 IP 每分钟请求上限 | `120` |

## 6. 本地运行

### 6.1 启动顺序

1. 启动方言识别服务
2. 启动后端
3. 启动前端

方言识别：

```bash
cd deploy/funasr
bash run.sh
```

后端：

```bash
cd backend
npm install
cp .env.example .env
npm run dev
```

前端：

```bash
cd frontend
npm install
npm run dev
```

访问：

- 前端：`https://localhost:5173`
- 后端：`http://127.0.0.1:3001`
- FunASR：`http://127.0.0.1:10095`

### 6.2 生产模式

```bash
./start-production.sh
```

后端会同时托管 API 和 `frontend/dist` 静态文件。

## 7. 测试

### 7.1 后端测试

```bash
cd backend
npm test
```

当前覆盖：

- 输入校验
- 翻译方向
- 网络梗分类
- 英语俚语
- 安全拦截
- 音频与视频边界
- 反馈接口

### 7.2 前端测试与构建

```bash
cd frontend
npm test
npm run build
```

### 7.3 一键冒烟测试

```bash
./smoke.sh
```

该脚本会依次执行前端测试、构建、后端测试、接口检查、安全检查和音频/视频识别检查。

### 7.4 反馈分析

```bash
cd backend
npm run analyze:feedback
```

生成：

- `backend/data/feedback-report.md`
- `backend/data/feedback-rule-suggestions.json`

## 8. 安全设计

- 敏感词规则：`backend/services/safety.js`
- 敏感词配置：`backend/config/content-policy.json`
- 输入合法性检查：`backend/services/inputGuard.js`
- 可选语义安全：`backend/services/semanticSafety.js`
- CORS 白名单：`backend/server.js`
- 接口限流：`backend/server.js`
- 视频大小限制：`backend/routes/transcribe.js`
- 图片 OCR 预检：`backend/routes/moderate.js`

## 9. 演示素材

当前演示素材位于 `frontend/public/demo/`：

- `yue-elder.wav`：长辈式粤语“后生仔，唔好成日挂住玩手机，早啲瞓啦。”
- `yue-00020.wav`：年轻粤语“你好似瘦咗啲㖞！”
- `yue-01012.wav`：粤语长句
- `meme-yyds.png`：网络梗图，Picsum 底图 + `yyds` 文字
- `yue-00020.mp4`：Big Buck Bunny 视频底片 + 粤语音频

## 10. 部署包

部署包：

```text
yuyi-deploy-package.tar.gz
```

包含后端源码、前端构建产物、演示素材、OCR 语言包、部署脚本和文档。

排除项：

- `.env`
- `node_modules`
- 用户反馈数据

部署详情见 `部署手册.md`。

## 11. 已知限制

- 当前主要支持中文和英文输入。
- 方言语音识别依赖 FunASR 或兼容 ASR 服务。
- 大模型语义安全层默认关闭，打开会增加接口延迟。
- 翻译质量依赖大模型，复杂语境下仍可能需要人工判断。

## 12. 后续方向

- 增加关系对象选择，如对父母、祖辈使用不同语气。
- 增加多轮对话上下文。
- 增加更多方言和更细的方言识别。
- 增加译文音色选择。
- 增加反馈后台，持续优化规则和提示词。
